package hexlet.code;

import java.util.List;
import java.util.Map;

public class Formatter {

    public static String Format (List<Map<String, Object>> result, String format) {
        switch (format) {
            case "stylish" -> Stylish.format(result);
            case
        }
// как должны преобразовываться разные статусы, для каждой мапы нужно отформатировать в строку и добавить в результирующий.в самом начале создается стринг резалт и потом в неё дописываем
        // нужно написать switch, когда будут написаны разные форматы отображения и нужно написать его , чтобы выбрать формат в зависимости какой формат был передан
        // поле формат можно
   return null;
    }
}
